name: Build Xbox MSIX 0.1beta

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Add Windows SDK tools to PATH
        shell: pwsh
        run: |
          $sdkRoot = Join-Path ${env:ProgramFiles(x86)} 'Windows Kits\10\bin'
          $makeAppx = Get-ChildItem $sdkRoot -Filter 'makeappx.exe' -File -Recurse |
            Where-Object { $_.FullName -match '\\x64\\makeappx\.exe$' } |
            Sort-Object FullName | Select-Object -Last 1
          if (-not $makeAppx) { throw "x64 makeappx.exe not found under $sdkRoot" }
          $makeAppx.Directory.FullName | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Install vcpkg dependencies
        shell: pwsh
        run: |
          $env:VCPKG_ROOT = 'C:\vcpkg'
          & "$env:VCPKG_ROOT\vcpkg.exe" install sdl2:x64-uwp freetype:x64-uwp libpng:x64-uwp libjpeg-turbo:x64-uwp zlib:x64-uwp
          "VCPKG_ROOT=$env:VCPKG_ROOT" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Build and package
        shell: pwsh
        run: |
          .\xbox\build-cmake.ps1

      - name: Sign MSIX with a temporary development certificate
        shell: pwsh
        run: |
          $password = ConvertTo-SecureString 'krkr-xbox-dev' -AsPlainText -Force
          $cert = New-SelfSignedCertificate -Type Custom -Subject 'CN=KrkrXbox' -KeyUsage DigitalSignature -FriendlyName 'KRKR Xbox Development'
          Export-PfxCertificate -Cert $cert -FilePath out\krkr-xbox-dev.pfx -Password $password | Out-Null
          Export-Certificate -Cert $cert -FilePath out\krkr-xbox-dev-0.1beta.cer | Out-Null
          $signTool = Get-ChildItem "$env:ProgramFiles(x86)\Windows Kits\10\bin\*\x64\signtool.exe" | Sort-Object FullName | Select-Object -Last 1
          if (-not $signTool) { throw 'Windows SDK signtool.exe was not found.' }
          & $signTool.FullName sign /fd SHA256 /f out\krkr-xbox-dev.pfx /p krkr-xbox-dev out\KRKR-Xbox-0.1beta.msix
          Remove-Item out\krkr-xbox-dev.pfx -Force

      - name: Upload MSIX
        uses: actions/upload-artifact@v4
        with:
          name: KRKR-Xbox-MSIX-0.1beta
          path: |
            out/KRKR-Xbox-0.1beta.msix
            out/krkr-xbox-dev-0.1beta.cer
          if-no-files-found: error
